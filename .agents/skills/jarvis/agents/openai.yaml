interface:
  display_name: "Jarvis"
  short_description: "Repo-specific patterns and workflows for jarvis"
  default_prompt: "Use the jarvis repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true